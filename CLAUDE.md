@AGENTS.md

要動部署、Cloudflare Pages 設定、`functions/` 或 `public/_routes.json`：
先讀 `docs/deployment.md`（那裡有一個會燒光整個帳號 Functions 配額的雷）。

# 文章寫作風格規範

所有從本專案產出的文章，寫作風格必須模仿 https://yi-tienpan.blogspot.com/ 的既有文章。

## 核心原則

「用具體故事或案例驗證抽象論點」——理論永遠搭配案例，不孤立存在。

## 開頭

場景化切入：用微觀故事（對話、個人經歷、具體瞬間）引入宏觀議題，製造「為什麼」的懸念。
不用理論堆砌開場，不用「本文將探討……」。

## 段落節奏

長短交錯：短段落（1–2 句）作轉折或強調，中長段落做論述，不全篇等長。

## 句子風格

- 短句為主，並列結構（A、B、C）多於複雜從句
- 主動語態，修飾詞少，直白敘述
- 數字後立即解釋意義，不讓讀者自己猜測

## 論述方式

層層遞進：場景提問 → 背景說明 → 核心論點 → 案例驗證（2–3 個）→ 洞察收尾。
不用「先結論後理由」，用「層層揭示」。

## 結尾

模式歸納或洞察作結，**無 call-to-action**。
不說「讀者可以…」「歡迎留言」「敬請期待下一篇」。
最後一句要直接把歸納講出來，不繞彎、不含蓄留白、不用婉轉暗示收尾。

## 標題與子標題（一律照 SEO 設計）

網站上每一篇文章的主標題（`title`）與內文子標題（`##`）都要從搜尋角度設計，不能只求文學感。判準與寫法見 `docs/seo/playbook.md` 第 19 項；下面是每次下標必做的四件事：

- **主標題前段放主要關鍵字**：讀者實際會搜的實體詞（機型、地名、球隊、機構、事件名）排在前面，避免用只有讀完全文才懂的意象開頭
- **一篇文章鎖兩個讀者群**：專業關鍵字（例：波音 787、試飛員）與大眾／旅遊關鍵字（例：名古屋、中部國際機場、展館）同時出現在標題或前兩個子標題裡
- **長度**：主標題控制在約 30 個全形字以內，關鍵字擠在前 20 字，避免搜尋結果被截斷
- **子標題自帶關鍵字**：`##` 不要只寫「首飛那天」這種無實體詞的短語，補上人名、年份、機型或地名，讓每段自足、可被單獨引用
- **標題要讓人想知道答案**：關鍵字排好之後還有一關。標題要給讀者一個他想知道答案的問題，不是描述這篇文章做了什麼。「19 季數據看規則能不能壓住三分」是在講作業內容；「為了減少三分出手，歐洲聯賽三分線外移半公尺，有效嗎？」才是搜尋的人腦子裡真正的問題。下標最後唸一次標題，問自己有沒有想知道答案，沒有就重下

標點仍照〈格式審查員〉那條：含逗號或問句要收句號／問號，純短語不加。禁止關鍵字堆砌（playbook 第 15 項，實測負效果）。

## 反面清單（絕對禁止）

- 感嘆號
- 粗體或底線強調（靠段落結構製造層級）
- 命令語氣（「你應該」「必須去…」）
- 重複論點：每段只推進新資訊，不回顧前文
- 引言或結論只是重述標題
- 結論段落只是重複文章已講的事
- 數據堆砌但沒有解讀脈絡
- 段落長度全部均等（無節奏感）
- **AI 寫作痕跡（除非來源原文真的這樣用，否則一律不用）**：以下句式與套語是 AI 腔的標記，且常是 AI 自己加的詮釋、不是報告原文的講法，會讓文章偏離來源、讀起來像機器寫的。框架與比喻要貼著來源真正的用詞（報告講 cost-exchange ratio 就用「成本／成本比」，講 electromagnetic spectrum 就用「電磁頻譜」）。
  - 對比、轉折句式：「不是 X，而是 Y」「與其說 X，不如說 Y」「表面上 X，實際上 Y」「表面是 X，深層是 Y」「更深層地看」「你以為是 A，其實是 B」「關鍵不在於 X，而在於 Y」「真正的問題不是 X，而是 Y」「看似矛盾，其實合理」「看似 X，其實 Y」「X 的背後，是 Y」「這不僅是 X，更是 Y」「從來不是 X」「真正的 X，是 Y」
  - 重新框定（re-framing）當推進方式：上面這些對比句式的共同來源，是「提出常見看法 → 否定或修正它 → 端出更高層次的說法」這個模板。它靠語氣轉折製造「論點變深了」的感覺，實際上沒有增加新資訊。個別句式改寫後若骨架仍是這個模板，一樣算違規。對比只能服務於分析：要否定一個看法，先給出推導（區分、限定、反駁、修正），且被否定的 A 必須是真的有人主張的看法，不能立稻草人；也不可把普通現象包裝成悖論或反直覺來吸睛。推進論點的手段是直接判斷、具體例子、因果推導、必要的限定、可被反駁的說法。同一篇文章裡，有論證支撐的對比至多用一處。
  - 抽象比喻、花俏框架：「看不見的 X（骨幹／戰線／手）」「一筆帳／算不平的帳／帳單」「算術題」「藏著……的全部矛盾」「X 的縮影／拼圖／註腳」「雙面刃」「冰山一角」「最不浪漫卻最難反駁」「欄位」當比喻（「不在計數的欄位裡」；欄位只能指真實表格或資料庫的欄位）「擺在一起看」
  - 並列歸納句式：把材料排在一起、宣稱共同點自己浮現的寫法（「擺在一起看」「攤開來」「排開來」「抄在一起」「湊在同一個主題上」「拼在一起」「跟……對得上」「重複的詞就浮出來了」「交集就是……」）。共同點要由作者直接指認，寫出具體內容（哪個詞、哪個數字、哪個對象重複），不靠「排好之後答案自然出現」的姿態推進
  - 宣告式引子句：先用一句短宣告帶冒號（「身分查得出來：」「需求很明確：」「目的只有一個：」「規則委員會的說法很直白：」「報告講得很白：」「道理很簡單：」「他們想問的事很單純：」），再把內容補在後面。宣告句沒有資訊，刪掉它，直接寫內容。轉述來源的說法時特別容易犯，改成「規則委員會給的理由是……」這種直接寫出內容的句子
  - 抽象歸納句：把幾件事的共同點抽象成「條件／前提／基礎」再宣告（「兩次復原都建立在同一個條件上」）。共同點要用具體事實直接寫（人還在、圖紙還在），不先蓋一層抽象名詞
  - 填充、轉場套語：「值得注意的是」「值得一提的是」「不容忽視的是」「顯而易見地」「事實上」「換句話說」「不難發現」「我們可以看到」「這意味著」「某種程度上」「更重要的是」「這個數字」回指式推進（「這個數字裡」「這個數字背後」；數字出現當下就解讀完，不用「這個數字」回頭指）
  - 自我複述定性句：一句話講完之後，再補一個小句把它重新定性一次（「十七處是破片傷，加上兩處槍傷就成了十九處，講的是同一次任務」）。補的那句沒有新資訊，只是把讀者剛讀懂的事再說一遍。要補就補新事實（例如那是哪一天的傷），不要補定性
  - 概括斷言代替具體事實：用一句概括的宣稱交代事情（「九個人打不動一場仗」），而不寫具體的東西。改成寫他們身上帶什麼、能叫到什麼、做不到什麼，讓讀者自己得出那個概括
  - 前後互相推翻而不交代時序：前一句寫成功、下一句用「後來還是」推翻（「第三次拉升成功。這架直升機後來還是迫降了」），讀者不知道到底成了沒有。同一件事的結果只能有一個，過程要按時序寫完（離地、飛出去、之後在航程中迫降）
  - 引文預告句：直接引語前不加一句敘述句預告引文內容（例如先寫「它拿得出兩樣東西。」再引「我們靠的就是兩樣東西……」），資訊重複；引文直接出場，或前置句只給說話者身分與情境
  - 引文翻譯要像口語：直接引語譯成中文後要唸得出口、像那個人會講的話（"bring to the table" 譯「我們靠的就是」而非「我們能拿出來的東西有」），不確定時先唸一遍
  - 開場套語：「在當今／在這個 X 的時代」「隨著 X 的發展」「本文將探討」「眾所周知」
  - 收尾套語：「總結來說」「綜上所述」「歸根結底／歸根究柢」「說到底」「答案或許就藏在……」「時間會給出答案」「這值得我們深思」
  - 贅詞與翻譯腔：「進行一個……的動作」「做一個……」、英式被動句堆疊、程度副詞連發（「極為」「相當」「非常」接連出現）
  - 結構痕跡：三連排比硬收尾、連續設問句堆砌、過度對仗工整、結論段只重述前文
  - 詞語誤用、用詞太重：別把字硬塞進不合的位置。例如「口徑」指統計定義或說法，不可拿來當「另一組資料」的代稱（要寫「另一組資料」就直接寫）；「更重」「講得更重」易讓人誤以為事態嚴重，若意思是「講得更多、更進一步」就寫「更進一步」「範圍更大」。
  - 同一說法整篇只用一次：有特色的詞或比喻（例如「差一個量級」），全篇用過一次就好，要再表達同個意思就換句話講，不要反覆出現同一個詞或句型。
- 破折號（——、—）：改用逗號、句號、分段或其他方式表達停頓與轉折；破折號是 AI 寫作的常見標記（此項無例外，一律禁用）
- **中國用語（除非引文原文如此，否則一律不用）**：文章用台灣慣用語。已知對照：「活」當「工作、事情」講（幹活、練的是……的活、活兒）→「事」「工作」「本事」；「視頻」→「影片」；「信息」→「資訊」；「質量」（指品質時）→「品質」；「軟件／硬件」→「軟體／硬體」；「網絡」→「網路」；「水平」（指程度時）→「水準」；「立馬」→「立刻」；「靠譜」→「可靠」；「算法」（單獨使用時）→「演算法」；「可視化」→「視覺化」；「貝葉斯」→「貝氏」（Bayesian 的台灣譯名，如貝氏統計、貝氏迴歸）；「智能體／多智能體」→「代理人／多代理人」（agent 的台灣譯名）；「承托板／承托」（球鞋部件）→「碳纖維板」「抗扭片」「支撐」；「車間」（工廠廠區）→「廠房」「工場」（「車間距」指行車間距，不在此限）。發現新的中國用語，加進這份對照並同步加正則。

寫作或審稿時對「重新框定骨架」拿不準，先讀 `docs/writing-style-examples.md`：裡面有四步判別法和違規／合格改寫的對照例。

---

# 執行行為準則

## 先確認再動手

接到任何需要大量輸出的任務（寫新文章、重大改寫、搜尋資料後整合），若需求有模糊之處，必須先說出自己的假設並等待確認，不得默默開始。

若有多種合理詮釋方向，主動列出選項讓使用者選擇，再開始執行。

## 精準修改，不擴散

被要求修改文章的特定部分（某一段、某個數字、某個標題），只改被指定的地方。不順手調整其他段落的措辭、標點、結構或格式，即使覺得那些地方「可以更好」。

已通過審查的文章尤其如此：局部修正不應觸動其他已確認的內容。

## 規則維護協議

- 寫作規範有改動（新增禁用句式、比喻、詞語誤用）時，必須同步更新 `scripts/check-post.mjs` 的對應正則，兩者放同一個 commit；只改其中一邊視為未完成。
- 修改本檔或 `.claude/` 底下的規則檔，只能新增條目或修正明確錯誤；要刪除或重寫既有規則，先徵得使用者同意。
- 踩坑教訓的去處：影響文章產出的具體規則，寫進本檔反面清單並加正則；跨 session 的工作方式教訓，寫進 memory 的 `feedback_*` 檔。寫入前先確認反面清單與 memory 沒有已涵蓋的重複條目。

---

# 文章發布流程規範

## 文章小隊 — 發文前必須執行

每當要 commit + push 一篇新文章（或更新文章）到 skyfaring 網站之前，
**必須先啟動文章小隊審查流程**，未通過不得發文。

### 啟動方式

發文前用兩個 subagent 同時平行審查，再由我（文章隊長）彙整：

---

### 隊員 1：格式審查員

審查以下項目，列出所有問題：

1. **YAML frontmatter** 是否齊全：
   - 必填欄位：`title`、`author`、`date`、`slug`、`tags`（至少一個）、`excerpt`
   - **`author` 固定為** `AI 初稿 / skyfaring 編輯校正`（除非使用者另有指示）。不要從個別舊文複製成 `Skyfaring` 等其他值
   - **每篇文章都必須有** `heroImage`、`heroAlt`、`heroCredit`、`heroCreditUrl`
   - **每篇文章都必須有** `highlight`（文章中段的重點引文，缺少時頁面不會渲染該區塊）
   - 有來源時應有 `source` 或 `references`；`source_url` 是合法欄位（讓來源變成可點擊連結），不需移除
   - `slug` 必須和檔名一致

2. **Markdown 結構**：
   - 文章不能有重複的段落標題
   - 不能有「本文資料來源」純文字區塊（來源由 frontmatter 的 `source`/`references` 處理）
   - 標題層級合理（文章內用 `##` 以下，不用 `#`）
   - **標題標點**：主標題（`title`）與內文子標題（`##`）若句中含逗號，或本身是問句，結尾必須補上句號或問號；純短語、無逗號、非問句的標題則維持不加結尾標點。例如「面對台海的那支艦隊，背後是同一個人。」（含逗號要句號）、「機器能預測 NBA 比賽結果嗎？」（問句要問號）

3. **語言與排版**：
   - 繁體中文
   - 程式碼、數字、縮寫格式一致
   - **中英文之間必須有半形空格（盤古之白）**：包含 `title`、`excerpt`、`highlight` 等 frontmatter 欄位，以及文章內文。例如「NBA 球員」、「ACL 重建」、「第 1 年」，不得寫成「NBA球員」、「ACL重建」

4. **AI 寫作痕跡掃描**：對照上方〈反面清單〉的「AI 寫作痕跡」逐項檢查全文（對比句式、重新框定推進、抽象比喻框架、填充轉場套語、開場與收尾套語、贅詞翻譯腔、結構痕跡、破折號）。發現一律列為待修，**除非來源原文真的這樣用**。重新框定要檢查的是段落骨架，不只字面：一段若是「先講一個看法、再否定它、再給更高層次說法」而中間沒有推導，即使沒用到清單裡的句型也要列為待修。

---

### 隊員 2：資料查核員

審查以下項目，列出所有問題：

1. **數據正確性**：文章中引用的數字、統計值是否與來源 PDF / 論文 / 資料一致
2. **來源標示**：有引用的說法是否都有對應來源
3. **邏輯一致性**：文章前後引用的數字有無矛盾
4. **資料時效**：時間相關的數據是否標示清楚（賽季、年份等）

---

### 文章隊長（我）彙整

收到兩位隊員的報告後：

- **有問題** → 列出待修清單，**暫停發文**，等修改完成後重新審查
- **全部通過** → 核准發文，執行 commit + push（push 後 Cloudflare Pages 會自動部署，文章才會出現在網站上）

---

## 配圖流程

每篇文章都必須有 hero 圖片，圖片存放於 `skyfaring/public/images/`。

**圖片來源與處理方式：**

- 使用者在本機 session 時，可提供圖片連結，由 Claude 用 `curl` 下載
- **使用者不在本機時（例如手機）**：Claude 應主動用 WebSearch 在公開授權圖庫搜尋合適圖片，確認授權與攝影師姓名後，直接用 `curl` 下載，不需使用者提供連結
- 可使用的圖庫（皆為免費商用授權）：Unsplash、Pexels、Pixabay、Wikimedia Commons 等
- 檔名使用文章 slug 加上描述性後綴，例如 `nba-early-sport-specialization-football.jpg`
- `heroImage` 路徑格式：`/images/檔名.jpg`
- **每篇文章的 hero 圖片不得與其他已發布文章重複使用同一張圖**

**依題材選圖：**

- **地域相關題材（台灣、某國、某地區）**：優先找該地實景 ＋ 主題相關的圖。找不到「地域＋主題」都對得上的，才退而求其次用「主題相關但拍攝地不符」的示意圖，並在回報時明講這張不是當地實景、搜過哪些方向才放棄。例：台灣災防題材，先找台灣的氣象雷達站、台灣搜救隊，找不到才用外國的同類設施
- **職業賽事相關（NBA、職籃、職業聯賽分析）**：選室內球館的球場大景。避免室外球場、車道／公園籃框、單一球員特寫。也避免有明顯大學校隊識別的球館（校名、校隊旗幟、NCAA 字樣），那是學生題材的視覺語言
- **學生／業餘題材（校隊分析、青少年運動、專項化研究）**：室外球場、有球員入鏡的照片才合適
- 下載後一律用 Read 開圖確認實際內容，不能只信搜尋結果的敘述或 alt text
- 使用者自拍照片的裁切、直式轉橫幅、統一光線、圖說樣式，照 `docs/photo-pipeline.md` 的管線處理

---

## 文章存檔規範

- **論文導讀 / PDF 翻譯類文章**：發文後同步存一份 `.md` 到 `skyfaring/articles/`
- **一般賽後分析文章**：不需存 `articles/`

這一步由 `scripts/hooks/post-push-archive.mjs` 自動完成：`git push` 成功後，看這次推上去的文章，frontmatter 的 `source`／`source_url`／`references`／`doi` 指向 arXiv、DOI、期刊與機構典藏，或智庫與國際組織（IISS、IFRI、CSIS、RUSI、Carnegie、CRS、DSET、ICAO、IATA、CFE-DMHA 等）就複製到 `articles/`，不用手動 `cp`。新聞報導、官方聲明、一般網址不算，要存就手動複製，或把網域加進該檔的 `ARCHIVABLE_SOURCE`。存檔比對的是 frontmatter 的 `slug` 而非檔名，所以帶日期前綴的舊存檔會被就地更新，不會變成兩份。`articles/` 是指向 OneDrive 的 junction，新機器沒建連結時 hook 會回報並略過。

## 專案位置（程式碼與資料分兩邊）

程式碼走 git，論文與存檔走 OneDrive。這兩類東西的同步方式不同，不要混在一起。

- **repo 本體**：各機器自行 `git clone` 到本機硬碟，路徑每台不同（A 機 `E:/repos/skyfaring`、B 機 `D:/repos/skyfaring`）。不要放進 OneDrive，OneDrive 按單一檔案同步，會讓 `.git` 的分支指標與物件對不上，造成物件遺失與分支回退
- **文章存放於**：`content/posts/`

## 論文與存檔資料夾（不在 git 裡）

以下資料夾的**實體檔案在 OneDrive**（所以各機器仍同步得到），但 repo 底下建了同名的目錄連結（Windows junction），在 repo 內用相對路徑就讀得到，不需要記兩套路徑：

- **無人機論文**：`drone-papers/`
- **籃球論文**：`basketball-papers/`
- **戰爭論文**：`warfare-papers/`
- **論文導讀存檔**：`articles/`
- **海事報告**：`maritime-reports/`

新機器 clone 之後這些連結不會自動存在，要自己建。在 repo 根目錄跑一次就好：

```
node scripts/setup-junctions.mjs
```

它從 `%OneDrive%` 推出來源路徑，一次建齊上面六個連結，已存在的不動。OneDrive 裝在別的位置時用參數指定：`node scripts/setup-junctions.mjs "D:\OneDrive\02_創作\14_AI TEST\skyfaring"`。手動建的話等同於：

```
mklink /J "<repo>\drone-papers" "<OneDrive>\02_創作\14_AI TEST\skyfaring\drone-papers"
```

每篇論文對應一個 `.md` 摘要檔（籃球論文另附 PDF），檔名格式：`YYYY-MM-DD-識別碼.md`

---

## 路由：派工／判斷 → 先讀哪份

- 新網站要做 SEO／GEO、或定期更新搜尋優化研究 → `docs/seo/README.md`（進入點：`/seo-audit`、`/seo-refresh`）
- 要不要派 subagent、模型怎麼選、怎麼驗收、本環境的 hook／部署限制 → `docs/agents/dispatch.md`
- 派工 prompt 直接套模板 → `docs/agents/templates.md`
- 何時算完成／何時問使用者／何時換路／何時升級模型 → `docs/agents/judgment.md`
- 要改這幾份制度檔本身（含本檔）→ `docs/agents/maintenance.md`
